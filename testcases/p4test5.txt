***loops

global len = 9;
global leno = 3;
global t = 0;

global arr;
array arr[1..8] kk = 9-kk/2;

t = len;
len = leno;
leno = t;

len<->leno;

print leno;
print len;


foreach i in 1..len-1 do
    foreach j in 1..len-i-1 do
        if arr[j]>arr[j+1] then
            t = arr[j];
            arr[j] = arr[j+1];
            arr[j+1] = t;
        end if
    end for
end for

